% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/variable_selection.R
\name{np_rank_correlations}
\alias{np_rank_correlations}
\title{Rank candidate predictors by absolute correlation with the dependent variable}
\usage{
np_rank_correlations(dta_trans, dep_var, candidates = NULL)
}
\arguments{
\item{dta_trans}{Data frame as returned by \code{\link{np_transform_data}}.}

\item{dep_var}{Character. Name of the dependent variable column.}

\item{candidates}{Character vector of candidate predictor column names to
evaluate.  If \code{NULL} (default), all columns except \code{date} and
\code{dep_var} are used.}
}
\value{
A data frame with columns:
\describe{
  \item{\code{variable}}{Candidate predictor name.}
  \item{\code{correlation}}{Pearson \eqn{r} with \code{dep_var}
    (signed, full sample).}
}
Rows are sorted by \eqn{|r|} descending.
}
\description{
Computes Pearson correlation between each candidate predictor and the
dependent variable over the full available sample (pairwise complete
observations), then returns a data frame ranked by \eqn{|r|} in
descending order.
}
\examples{
\dontrun{
corr_tbl <- np_rank_correlations(dta_trans, dep_var = "im_SA")
head(corr_tbl, 10)
}

}
